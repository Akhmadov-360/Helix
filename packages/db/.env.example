# Prisma CLI (migrate/generate/studio) reads DATABASE_URL from this file.
# Port 5433 — docker-compose maps postgres there to avoid clashing with a
# system PostgreSQL on 5432 (see CLAUDE.md Gotchas).
DATABASE_URL="postgresql://helix:helix@localhost:5433/helix?schema=public"
